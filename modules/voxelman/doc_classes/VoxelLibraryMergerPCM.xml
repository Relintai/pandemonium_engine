<?xml version="1.0" encoding="UTF-8" ?>
<class name="VoxelLibraryMergerPCM" inherits="VoxelLibrary">
	<brief_description>
		Voxel library implementation that merges all textures from it's surfaces per chunk.
	</brief_description>
	<description>
		Voxel library implementation that merges all textures from it's surfaces, but unlike [VoxelLibraryMerger] this class creates textures per chunks.
		Chunks are evaluated and material sets are only created once. For example if you have 10 chunks which needs terrain surface texture 1,2 and 10 which needs 3,4 you will only have 2 merged textures with materials, and the chunks will share them.
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="get_prop_packer">
			<return type="TexturePacker" />
			<description>
				The prop packer.
			</description>
		</method>
		<method name="get_prop_uv_rect">
			<return type="Rect2" />
			<argument index="0" name="texture" type="Texture" />
			<description>
				Returns uv rect for the given texture.
			</description>
		</method>
	</methods>
	<members>
		<member name="background_color" type="Color" setter="set_background_color" getter="get_background_color" default="Color( 0, 0, 0, 1 )">
			The merged texture's background_color setting.
		</member>
		<member name="keep_original_atlases" type="bool" setter="set_keep_original_atlases" getter="get_keep_original_atlases" default="false">
			The merged texture's keep_original_atlases setting.
		</member>
		<member name="margin" type="int" setter="set_margin" getter="get_margin" default="0">
			The merged texture's margin setting.
		</member>
		<member name="max_atlas_size" type="int" setter="set_max_atlas_size" getter="get_max_atlas_size" default="1024">
			The merged texture's max_atlas_size setting.
		</member>
		<member name="props" type="Array" setter="set_props" getter="get_props" default="[  ]">
			Props.
		</member>
		<member name="texture_flags" type="int" setter="set_texture_flags" getter="get_texture_flags" default="5">
			Voxel surfaces.
		</member>
		<member name="voxel_surfaces" type="Array" setter="set_voxel_surfaces" getter="get_voxel_surfaces" default="[  ]">
			The merged texture's texture_flags setting.
		</member>
	</members>
	<constants>
	</constants>
</class>
