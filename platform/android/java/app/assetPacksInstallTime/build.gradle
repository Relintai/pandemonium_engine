plugins {
    id 'com.android.asset-pack'
}

assetPack {
    packName = "assetPacksInstallTime" // Directory name for the asset pack
    dynamicDelivery {
        deliveryType = "install-time" // Delivery mode
    }
}
